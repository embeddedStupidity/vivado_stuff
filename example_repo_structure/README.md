# Example Repo Structure

This is meant to srve as an example of how to structure a SoC project's Git repo to take advantage of the automated scripts. The user's FPGA repo **must** follow this structure for the create_project.tcl script to work.
